# 📊 RQ_MobCounter

Утилита для анализа логов чата Royal Quest. Парсит HTML файлы логов, подсчитывает статистику убийств монстров и полученного опыта.

## 🚀 Быстрый старт

### ⚠️Внимание! Настройте Royal Quest, чтобы он сохранял логи⚠️
В Роял Квесте: Настройки → Чат → Название чата (например exp) → ✓ Сохранять историю сообщений  

В некоторых случаях чтобы история сохранилась нужно будет выйти в выбор персонажа, а потом из игры

### 1️⃣ Скачайте релиз

Скачайте последний релиз с GitHub и распакуйте в удобную папку, например `C:\Games\RQ_MobCounter`

### 2️⃣ Добавьте папку в PATH

**Windows 10 и выше:**
1. Нажмите `Win + X` и выберите "Параметры системы" (System Settings)
2. Перейдите в **Параметры** → **Система** → **О программе** → **Дополнительные параметры системы**
3. Нажмите кнопку **Переменные среды** (Environment Variables)
4. В разделе **Переменные пользователя** найдите переменную `Path` и нажмите **Изменить**
5. Нажмите **Создать** (New) и добавьте путь к папке с приложением, например:
   ```
   C:\Games\RQ_MobCounter
   ```
6. Нажмите **OK** и **Применить**
7. Перезагрузитесь или откройте новое окно командной строки

**Windows 7:**
1. Откройте **Панель управления** → **Система и безопасность** → **Система**
2. Нажмите **Дополнительные параметры системы**
3. Нажмите **Переменные среды** (Environment Variables)
4. Отредактируйте переменную `Path` и добавьте путь через точку с запятой (`;`)
5. Нажмите **OK** и перезагрузитесь

### 3️⃣ Готово! Теперь вы можете запускать:

```bash
rqmc --exp
```

Из любой папки в командной строке!

▶️ **[Видео инструкция на YouTube](https://www.youtube.com/@Lion-programmer)** (скоро)

## 📖 Использование

### Основные команды

```bash
# Анализ текущего месяца с опытом
rqmc --exp

# Только количество убийств
rqmc

# Конкретный месяц
rqmc --month=2026.01 --exp

# Все месяцы за раз
rqmc --all --exp

# Сортировка по опыту, топ 10
rqmc --exp --sort=exp --limit=10

# Сортировка по количеству, лимит 50
rqmc --sort=count --limit=50
```

### Флаги

| Флаг | Описание |
|------|---------|
| `--exp` | Показывать опыт (без флага показывает только имя и количество) |
| `--month=YYYY.MM` | Анализ конкретного месяца, например `2026.01` |
| `--all` | Обработка всех файлов логов |
| `--sort=count\|exp` | Сортировка: `count` (по количеству, по умолчанию) или `exp` (по опыту) |
| `--limit=N` | Максимальное количество записей для отображения (по умолчанию 20) |

## ⚙️ Конфигурация

Приложение автоматически ищет файл `config.json` в своей папке. Если файл не найден, приложение выдаст предупреждение с инструкциями как его создать. Так же вы можете установить флаги по умолчанию отредактировав `rqmc.bat`

### Структура config.json

```json
{
  "log_path": "D:\\B.A.S.E\\Games\\Royal Quest\\chatlogs",
  "file_prefix": "exp"
}
```

**Параметры:**
- `log_path` - путь к папке с логами Royal Quest
- `file_prefix` - префикс файлов логов (обычно `exp`, но может быть другой)

## 📋 Пример вывода

```
Монстр                                   |      Количество |  Суммарный опыт
---------------------------------------------------------------------------
Злая шкатулка                            |               8 |           22984
Ледяной слизняк                          |               6 |            9300
Часы                                     |               4 |           70120
Росинка                                  |               3 |               9

Всего записей: 32
Всего опыта: 102413
```

## 💻 Требования

- Windows 7 и выше (или Linux/macOS)
- Файлы логов в формате `.htm` из папки Royal Quest

## ❓ Проблемы

Если приложение не находит ваши логи:

1. ✓ Проверьте, что файлы логов названы в формате: `{prefix} (YYYY.MM).htm` (например, `exp (2026.01).htm`)
2. ✓ Проверьте параметр `log_path` в `config.json`
3. ✓ Убедитесь, что папка с логами существует и содержит `.htm` файлы
4. ✓ Проверьте, что параметр `file_prefix` в `config.json` совпадает с названием ваших файлов
5. В крайнем случае напишите мне в ТГ [Linsaym397](https://t.me/Linsaym397)

## 👨‍💻 Для разработчиков

Если вы хотите собрать приложение или внести изменения, смотрите [DEVELOPMENT.md](DEVELOPMENT.md)

---

**RQ_MobCounter** - открытая утилита для анализа логов Royal Quest
